#15-1 1208. Get Equal Substrings Within Budget
int equalSubstring(char* s, char* t, int maxCost) {
    int N = strlen(s); //先知道字串的長度
    for(it i=0;i<N;i++)//字串的迴圈
    {
        printf("%c %c\n",s[i],t[i]);//字串的陣列
    }
    return 0;
}

#week15-1.py(寫到一半)
class Solution:
    def equalSubstring(self, s: str, t: str, maxCost: int) -> int:
        #先用 python 把字串的回寫出來
        N = len(s) #先知道字串的長度
        for i in range(N):#字串迴圈
            print(s[i],t[i])#字串陣列
        return 0
